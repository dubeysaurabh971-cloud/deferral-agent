.venv/
__pycache__/
*.pyc
.env
data/chroma/
data/kb/chunks.jsonl
traces/*.jsonl
.pytest_cache/

# harness scratch output -- latest run only; named archives in eval_results/ are the record
eval_results/gated_full.json
eval_results/gated_sampled.json

# generated: the explorer regenerates these from traces/runs.jsonl
# (kept in git so GitHub Pages can serve them without a build step)

# Space build output (regenerate with: python -m deploy.build_space)
space_build/
